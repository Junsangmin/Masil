# 마실(가제) 서비스 기획안

## 1. 서비스 개요

**마실(가제)**은 사용자가 길을 떠나는 매 순간을 자신만의 방식으로 즐길 수 있도록 돕는 **자연어 기반 개인 맞춤형 길찾기 서비스**이다. 기존 길찾기 서비스가 주로 최단 시간, 최소 환승, 최소 도보와 같이 정해진 기준에 따라 경로를 제공했다면, 마실은 사용자의 성향과 현재 상황을 함께 반영하여 더 현실적이고 개인화된 이동 경로를 추천하는 것을 목표로 한다.

마실은 단순히 “가장 빠른 길”을 알려주는 서비스가 아니라, 사용자가 원하는 이동 경험을 자연어로 입력하면 그 의도를 분석해 여러 경로 후보를 비교하고, 사용자에게 가장 적합한 경로와 추천 이유를 제공한다.

예를 들어 사용자는 다음과 같이 요청할 수 있다.

```text
조금 늦어도 괜찮으니까 덜 걷는 길로 가고 싶어.
환승이 적은 길로 추천해줘.
비 오는 날이라 최대한 실내 이동 위주로 가고 싶어.
막차가 끊기기 전에 갈 수 있는 데까지 대중교통을 타고, 이후에는 택시를 최소 비용으로 이용하고 싶어.
나는 걷는 걸 싫어해서 도보 거리가 짧은 길이 좋아.
나는 빠른 게 제일 중요해.
```

마실은 이러한 자연어 요청을 분석하여 사용자의 선호 조건을 시간, 도보 거리, 환승 횟수, 쾌적도, 비용 등의 기준으로 변환하고, 대중교통 경로 후보를 점수화하여 최종 추천 경로를 제공한다.

## 2. 문제 정의

현재의 주요 길찾기 서비스는 대중교통 경로 탐색에 있어 높은 완성도를 가지고 있지만, 대부분의 추천 기준은 비교적 고정적이다. 사용자는 보통 최단 시간, 최소 환승, 최소 도보와 같은 제한된 선택지 안에서 경로를 고르게 된다.

하지만 실제 이동 상황에서는 단순한 시간보다 더 중요한 조건이 존재한다.

예를 들어 어떤 사용자는 조금 늦더라도 덜 걷는 경로를 선호하고, 어떤 사용자는 무조건 빠른 경로를 원한다. 또 비가 오는 날에는 실외 도보를 줄이는 것이 중요할 수 있고, 막차 시간이 애매한 상황에서는 “대중교통으로 갈 수 있는 곳까지 간 뒤 택시를 최소 비용으로 이용하는 경로”가 더 현실적인 대안이 될 수 있다.

즉, 기존 길찾기 서비스는 경로 정보 자체는 잘 제공하지만, 사용자의 성향과 현재 상황을 충분히 반영한 “개인 맞춤형 의사결정”에는 한계가 있다.

마실은 이러한 문제를 해결하기 위해 사용자의 자연어 입력을 기반으로 이동 선호와 상황을 분석하고, 여러 경로 후보를 비교하여 사용자에게 가장 적합한 경로를 추천한다.

## 3. 서비스 목표

마실의 목표는 사용자의 현재 상황에 가장 잘 맞는 길찾기 경험을 제공하는 것이다.

주요 목표는 다음과 같다.

```text
1. 자연어 기반 사용자 맞춤 길찾기 제공
2. 사용자의 성향과 이동 선호를 반영한 경로 추천
3. 날씨, 막차, 도보 부담 등 상황 기반 경로 추천
4. 여러 경로 후보의 점수화 및 비교 제공
5. 추천 경로에 대한 이유 설명 제공
6. 향후 앱 서비스로 확장 가능한 모바일 웹앱 구조 구현
```

## 4. 핵심 기능

### 4.1 자연어 기반 경로 요청

사용자는 버튼 선택뿐만 아니라 자연어 문장으로 원하는 이동 방식을 입력할 수 있다.

예시는 다음과 같다.

```text
덜 걷고 싶어.
환승이 적은 길이 좋아.
조금 늦어도 편하게 가고 싶어.
비를 맞기 싫어서 실내 위주로 이동하고 싶어.
막차가 끊길 것 같은데 택시비를 최소화하고 싶어.
```

서비스는 해당 문장을 분석하여 사용자가 중요하게 생각하는 조건을 추출한다.

### 4.2 사용자 선호 기반 경로 점수화

대중교통 API를 통해 여러 경로 후보를 가져온 뒤, 각 경로를 다음 기준으로 비교한다.

```text
소요 시간
도보 거리
환승 횟수
예상 비용
쾌적도
상황 적합도
```

이후 사용자의 요청에 따라 각 기준의 가중치를 다르게 적용한다.

예를 들어 “덜 걷고 싶어”라는 요청이 들어오면 도보 거리의 가중치를 높이고, “빠른 게 좋아”라는 요청이 들어오면 소요 시간의 가중치를 높인다.

### 4.3 추천 이유 제공

단순히 경로 하나를 추천하는 것이 아니라, 왜 해당 경로가 사용자에게 적합한지 설명한다.

예시는 다음과 같다.

```text
추천 경로는 최단 경로보다 6분 늦지만 도보 거리가 430m 짧고 환승이 1회 적어, 사용자의 “덜 걷고 환승 적은 길” 요청에 더 적합합니다.
```

이를 통해 사용자는 추천 결과를 신뢰하고, 경로 선택의 이유를 이해할 수 있다.

### 4.4 상황 기반 길찾기 고도화

마실은 1차적으로 자연어 기반 개인 맞춤 길찾기를 제공하고, 2차적으로 기존 길찾기 서비스에서 아쉬웠던 상황 대응 기능을 개선한다.

대표적인 예시는 다음과 같다.

```text
막차 상황:
대중교통으로 갈 수 있는 지점까지 이동한 뒤, 이후 택시를 이용했을 때의 최소 비용 경로 추천

비 오는 날:
실외 도보를 줄이고 지하상가, 역사 내부 이동 등 실내 이동 가능성이 높은 경로 우선 추천

사용자 성향:
걷기 싫어하는 사용자, 빠른 이동을 선호하는 사용자, 환승을 싫어하는 사용자 등 개인 성향 반영
```

## 5. 전체 시스템 구조

```text
사용자
→ React 기반 모바일 웹앱(PWA)
→ FastAPI 백엔드 서버
→ Amazon Bedrock 자연어 분석
→ NAVER 장소 검색 / Geocoding API
→ ODsay 대중교통 API
→ Python 경로 점수화 알고리즘
→ RDS PostgreSQL 저장
→ 추천 경로 및 추천 이유 반환
```

## 6. 프론트엔드 기술스택

### 6.1 사용 기술

```text
Frontend Framework: React
Build Tool: Vite
Language: TypeScript
Styling: Tailwind CSS
Service Form: PWA
Map Visualization: NAVER Maps JavaScript API
API Communication: Axios 또는 Fetch
State Management: React Hooks 또는 Zustand
```

### 6.2 선정 이유

본 서비스는 사용자가 모바일 환경에서 출발지, 도착지, 자연어 요청을 입력하고 추천 경로를 확인하는 앱 서비스에 가깝다. 다만 MVP 단계에서 Android/iOS 네이티브 앱을 각각 개발하는 것은 개발 부담이 크기 때문에, 우선은 모바일 웹앱 기반 PWA로 구현한다.

React와 Vite를 사용하면 빠르게 프론트엔드 화면을 개발할 수 있으며, TypeScript를 적용하면 백엔드에서 전달받는 경로 데이터, 점수 데이터, 추천 결과 데이터의 타입을 안정적으로 관리할 수 있다. Tailwind CSS는 모바일 중심의 카드형 UI와 지도 기반 화면을 빠르게 구성하는 데 적합하다.

지도 시각화는 NAVER Maps JavaScript API를 활용한다. 이를 통해 출발지와 도착지 마커, 선택된 경로의 시각화, 추천 경로 강조 등의 기능을 제공할 수 있다.

기획안 기준으로는 다음과 같이 정리할 수 있다.

```text
본 서비스는 앱 서비스 성격을 가지지만, MVP 단계에서는 접근성과 개발 효율성을 고려하여 React 기반 모바일 웹앱(PWA)으로 구현한다. 이후 서비스 고도화 단계에서 Capacitor 또는 React Native를 활용해 Android/iOS 앱으로 확장 가능한 구조를 채택한다.
```

## 7. 백엔드 기술스택

### 7.1 사용 기술

```text
Language: Python
Backend Framework: FastAPI
Server: Uvicorn
Data Validation: Pydantic
External API Client: httpx
Database Integration: SQLAlchemy
Database Migration: Alembic
Environment Management: python-dotenv, AWS Parameter Store 또는 Secrets Manager
API Documentation: FastAPI Swagger / OpenAPI
```

### 7.2 FastAPI 선정 이유

백엔드는 프론트엔드에서 전달받은 출발지, 도착지, 자연어 요청을 처리하고, 외부 API를 호출한 뒤, 경로 데이터를 정규화하여 AI&알고리즘 모듈에 전달하는 역할을 담당한다.

본 서비스의 백엔드는 단순 CRUD 서버가 아니라 다음과 같은 흐름을 처리해야 한다.

```text
사용자 요청 수신
→ 장소 검색 및 좌표 변환
→ 대중교통 경로 API 호출
→ 경로 데이터 정규화
→ 자연어 선호 분석 결과 반영
→ Python 기반 경로 점수화 알고리즘 실행
→ 추천 경로 및 추천 이유 반환
```

이 구조에서는 Python 기반 FastAPI가 가장 적합하다. AI&알고리즘 파트가 Python으로 구현되기 때문에, 백엔드와 알고리즘을 같은 언어 환경에서 연결할 수 있다. 이를 통해 별도의 언어 간 연결 과정이나 알고리즘 서버 분리 없이, FastAPI 내부 모듈 형태로 추천 로직을 바로 호출할 수 있다.

Node.js 기반 백엔드도 외부 API 호출과 일반적인 웹 서버 구현에는 충분히 사용할 수 있다. 그러나 본 프로젝트에서는 경로 점수화, 선호 가중치 계산, 추천 이유 생성 등 AI&알고리즘 파트가 Python 중심으로 구현될 가능성이 높다. 이 경우 Node.js를 사용하면 Python 알고리즘을 별도 서버로 분리하거나, 알고리즘을 JavaScript/TypeScript로 다시 구현해야 하는 부담이 생긴다.

따라서 Node.js도 기술적으로는 가능하지만, 본 프로젝트의 팀 역할과 알고리즘 구조를 고려하면 FastAPI를 사용하는 것이 개발 복잡도를 줄이고 구현 효율을 높이는 선택이다.

기획안 기준으로는 다음과 같이 정리할 수 있다.

```text
백엔드는 Python 기반 FastAPI 프레임워크를 사용한다. 본 서비스는 대중교통 경로 데이터를 수집한 뒤 사용자 선호 기반 알고리즘을 통해 경로를 점수화하는 구조이므로, AI&알고리즘 모듈과의 연동성이 높은 Python 기반 백엔드가 적합하다. Node.js 기반 백엔드도 가능하지만, Python 알고리즘과의 직접적인 연결성과 개발 효율성을 고려하여 FastAPI를 채택한다.
```

## 8. AI&알고리즘 기술스택

### 8.1 사용 기술

```text
Language: Python
AI/NLP: Amazon Bedrock
Core Algorithm: Rule-based Preference Analysis + Route Scoring Algorithm
Numerical Calculation: NumPy
Data Processing: 기본 Python 자료구조 또는 pandas
Fallback Logic: 규칙 기반 자연어 분석
Backend Integration: FastAPI 내부 Python 모듈 연동
```

### 8.2 선정 이유

AI&알고리즘 파트는 서비스의 핵심 차별화 요소이다. 사용자의 자연어 요청을 분석하여 선호 조건을 추출하고, 이를 경로 점수화에 사용할 가중치로 변환한다.

예시는 다음과 같다.

```text
"빨리 가고 싶어" → 시간 가중치 증가
"덜 걷고 싶어" → 도보 거리 가중치 증가
"환승 싫어" → 환승 횟수 가중치 증가
"편하게 가고 싶어" → 쾌적도 가중치 증가
"비 맞기 싫어" → 실외 도보 패널티 증가
```

MVP 단계에서는 규칙 기반 자연어 분석과 가중치 기반 점수화 알고리즘을 우선 구현한다. 이를 통해 외부 LLM 응답 실패나 비용 문제와 관계없이 기본 추천 기능이 안정적으로 작동하도록 한다.

고도화 단계에서는 Amazon Bedrock을 활용하여 사용자의 자연어 요청을 더 정교하게 분석한다. Bedrock은 사용자의 문장을 시간, 도보, 환승, 쾌적도, 비용 등의 선호 가중치 JSON으로 변환하는 역할을 담당한다. 실제 경로 점수 계산과 최종 추천은 Python 알고리즘이 수행한다.

이렇게 역할을 분리하면 LLM에게 전체 추천 판단을 맡기는 것보다 안정적이다. Bedrock은 자연어 이해를 담당하고, Python 알고리즘은 명확한 기준에 따라 경로 점수를 계산한다.

### 8.3 알고리즘 처리 흐름

```text
1. 사용자 자연어 요청 입력
2. Bedrock 또는 규칙 기반 로직으로 선호 조건 추출
3. 선호 조건을 가중치로 변환
4. ODsay API에서 받은 경로 후보 정규화
5. 시간, 도보 거리, 환승 횟수, 비용, 쾌적도 기준으로 경로 점수 계산
6. 가장 높은 점수의 경로 선택
7. 대체 경로와 비교하여 추천 이유 생성
```

기획안 기준으로는 다음과 같이 정리할 수 있다.

```text
AI&알고리즘 파트는 Python을 기반으로 구현한다. 사용자의 자연어 요청을 Amazon Bedrock 또는 규칙 기반 fallback 로직으로 분석하여 시간, 도보 거리, 환승 횟수, 쾌적도 등의 선호 가중치로 변환하고, 대중교통 API에서 수집한 경로 후보를 정규화한 뒤 가중합 방식으로 점수를 계산한다. 이후 가장 높은 점수를 받은 경로를 추천 경로로 선정하고, 사용자의 요청과 대체 경로와의 비교를 바탕으로 추천 이유를 생성한다.
```

## 9. 외부 API 활용 계획

### 9.1 ODsay 대중교통 API

```text
역할: 출발지와 도착지 좌표를 기반으로 대중교통 경로 후보 수집
사용 위치: 백엔드
```

ODsay API는 본 서비스의 핵심 경로 후보 데이터를 제공한다. 백엔드는 사용자의 출발지와 도착지를 좌표로 변환한 뒤 ODsay 대중교통 API를 호출하여 여러 경로 후보를 가져온다.

### 9.2 NAVER 지역 검색 API

```text
역할: 사용자가 입력한 장소명을 실제 장소 후보로 변환
사용 위치: 백엔드
```

사용자가 “인하대학교”, “여의도역”, “강남역”과 같이 텍스트로 장소를 입력하면, NAVER 지역 검색 API를 활용하여 실제 장소 후보를 검색한다.

### 9.3 NAVER Geocoding API

```text
역할: 주소 또는 장소 정보를 위도·경도 좌표로 변환
사용 위치: 백엔드
```

선택된 장소의 주소 정보를 위도와 경도로 변환하여 대중교통 경로 API의 입력값으로 사용한다.

### 9.4 NAVER Maps JavaScript API

```text
역할: 지도 표시, 마커 표시, 추천 경로 시각화
사용 위치: 프론트엔드
```

프론트엔드에서는 NAVER Maps JavaScript API를 활용하여 지도 화면을 제공하고, 출발지와 도착지, 추천 경로를 시각적으로 표시한다.

### 9.5 Amazon Bedrock

```text
역할: 자연어 요청 분석 및 선호 가중치 JSON 변환
사용 위치: 백엔드
```

Amazon Bedrock은 사용자의 자연어 요청을 분석하여 경로 추천에 필요한 선호 조건을 추출하는 데 사용한다. 단, 서비스 안정성을 위해 Bedrock 응답 실패 시 규칙 기반 fallback 로직이 작동하도록 구성한다.

## 10. AWS 클라우드 기술스택

### 10.1 사용 기술

```text
Cloud Platform: AWS
Compute: EC2
Database: RDS PostgreSQL
Storage: S3
AI Service: Amazon Bedrock
Monitoring/Logging: CloudWatch
Network: VPC, Subnet, Security Group
Security: IAM
Secret Management: Secrets Manager 또는 Parameter Store
Deployment: Docker, Nginx, Uvicorn
Optional: Route 53, ACM, CloudFront
```

### 10.2 선정 이유

본 프로젝트는 AWS 수업 팀프로젝트이므로, 단순히 로컬에서 동작하는 서비스를 만드는 것이 아니라 클라우드 환경에서 실제 서비스 가능한 구조로 배포하는 것이 중요하다.

FastAPI 백엔드는 EC2 인스턴스에 배포한다. Docker를 사용하면 개발 환경과 배포 환경의 차이를 줄일 수 있고, Nginx를 리버스 프록시로 사용하여 외부 요청을 FastAPI 서버로 전달할 수 있다.

사용자 검색 기록, 추천 결과, 경로별 점수, 자연어 요청 분석 결과 등은 RDS PostgreSQL에 저장한다. 이를 통해 서비스 사용 데이터를 구조적으로 관리하고, 향후 개인화 추천 고도화에 활용할 수 있다.

S3는 프론트엔드 정적 파일, 이미지, 리소스 저장소로 활용할 수 있다. 필요에 따라 CloudFront를 연결하면 정적 파일을 더 빠르게 배포할 수 있다.

CloudWatch는 서버 로그, API 호출 에러, 추천 요청 처리 상태를 모니터링하는 데 사용한다. 이를 통해 단순 개발 결과물이 아니라 운영 가능한 클라우드 서비스 구조를 보여줄 수 있다.

IAM, Security Group, Secrets Manager 또는 Parameter Store는 보안 구성을 위해 사용한다. 외부 API Key, DB 접속 정보, Bedrock 접근 권한 등 민감한 정보는 코드에 직접 작성하지 않고 안전하게 관리한다.

### 10.3 AWS 구성 요약

```text
React PWA
→ S3 또는 EC2/Nginx를 통한 프론트 배포

FastAPI Backend
→ EC2에서 Docker 기반 실행

AI/NLP
→ Amazon Bedrock으로 자연어 요청 분석

Database
→ RDS PostgreSQL에 사용자 검색 기록 및 추천 결과 저장

Monitoring
→ CloudWatch로 서버 로그 및 API 상태 확인

Security
→ IAM, Security Group, Secrets Manager 또는 Parameter Store로 접근 권한 및 API Key 관리
```

기획안 기준으로는 다음과 같이 정리할 수 있다.

```text
클라우드 인프라는 AWS를 기반으로 구성한다. 백엔드 FastAPI 서버는 EC2 인스턴스에 Docker 기반으로 배포하고, 사용자 검색 기록과 추천 결과 데이터는 RDS PostgreSQL에 저장한다. AI 기능은 Amazon Bedrock을 활용하여 자연어 요청을 분석하며, 서비스 로그와 서버 상태는 CloudWatch를 통해 모니터링한다. 또한 VPC, Security Group, IAM을 활용해 네트워크 접근 범위와 리소스 권한을 제어하고, 외부 API Key와 DB 접속 정보는 Secrets Manager 또는 Parameter Store를 통해 안전하게 관리한다.
```

## 11. 최종 기술스택 요약

| 파트 | 기술스택 | 역할 |
|---|---|---|
| 프론트엔드 | React, Vite, TypeScript, Tailwind CSS, PWA | 모바일 웹앱 화면 구현, 사용자 입력, 추천 결과 표시 |
| 지도 시각화 | NAVER Maps JavaScript API | 지도 표시, 마커 표시, 경로 시각화 |
| 백엔드 | Python, FastAPI, Uvicorn, Pydantic, httpx | API 서버, 외부 API 호출, 데이터 정규화, 추천 결과 반환 |
| AI&알고리즘 | Python, Amazon Bedrock, NumPy, Rule-based Fallback | 자연어 분석, 선호 가중치 변환, 경로 점수화 |
| 대중교통 API | ODsay 대중교통 API | 대중교통 경로 후보 수집 |
| 장소/좌표 API | NAVER 지역 검색 API, NAVER Geocoding API | 장소 검색, 주소-좌표 변환 |
| 데이터베이스 | AWS RDS PostgreSQL | 검색 기록, 추천 결과, 경로 점수 저장 |
| 서버 배포 | AWS EC2, Docker, Nginx | FastAPI 서버 배포 및 운영 |
| 저장소 | AWS S3 | 정적 파일 및 리소스 저장 |
| 모니터링 | AWS CloudWatch | 서버 로그, 에러 로그, API 상태 확인 |
| 보안 | IAM, Security Group, Secrets Manager 또는 Parameter Store | 권한 관리, 네트워크 제어, API Key 관리 |

## 12. 기대 효과

마실은 기존 길찾기 서비스가 제공하는 고정된 추천 기준을 넘어, 사용자의 성향과 현재 상황을 반영한 개인 맞춤형 이동 경험을 제공한다.

특히 자연어 기반 입력을 통해 사용자는 복잡한 옵션을 직접 설정하지 않아도 원하는 이동 방식을 쉽게 표현할 수 있다. 또한 추천 경로와 함께 추천 이유를 제공함으로써 사용자가 결과를 이해하고 신뢰할 수 있도록 한다.

기술적으로는 React 기반 모바일 웹앱, FastAPI 백엔드, Python 알고리즘, Amazon Bedrock, AWS 클라우드 인프라를 결합하여 실제 서비스 가능한 클라우드 기반 AI 길찾기 시스템을 구현한다.

최종적으로 마실은 단순히 목적지까지 가는 방법을 알려주는 서비스가 아니라, 사용자의 이동 성향과 상황을 이해하고 그 순간에 가장 적합한 길을 함께 찾아주는 개인 맞춤형 길찾기 서비스로 발전할 수 있다.
