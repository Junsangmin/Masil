# AI & 알고리즘 담당 역할 정리

## 한 줄 정의

> 사용자의 자연어 이동 요청을 분석하고, 여러 대중교통 경로 후보 중 사용자 선호에 가장 적합한 경로를 선택하는 추천 로직을 만드는 역할

AI & 알고리즘 담당은 단순히 “AI API를 붙이는 역할”이 아니다.  
이 프로젝트에서 기존 길찾기 서비스와 차별화되는 핵심은 **최단시간 경로가 아니라 사용자의 상황과 선호를 반영한 맞춤형 경로 추천**이다.

즉, AI & 알고리즘 담당은 아래 흐름을 담당한다.

```text
사용자 자연어 요청
 ↓
선호 조건 분석
 ↓
가중치 변환
 ↓
경로 후보 점수화
 ↓
최종 추천 경로 선택
 ↓
추천 이유 생성
```

---

# 1. AI & 알고리즘의 핵심 역할

AI & 알고리즘 담당은 백엔드가 전달한 사용자 요청과 경로 후보 데이터를 받아서,  
어떤 경로가 사용자에게 가장 적합한지 판단한다.

## 예시 입력

```text
출발지: 인하대학교
도착지: 강남역
요청: 조금 늦어도 덜 걷고 싶고 환승은 적었으면 좋겠어.
```

## AI & 알고리즘이 해야 하는 판단

```text
사용자는 빠른 도착보다 도보 거리와 환승 횟수를 더 중요하게 생각한다.
따라서 최단시간 경로가 아니더라도,
도보 거리가 짧고 환승 횟수가 적은 경로를 우선 추천해야 한다.
```

---

# 2. 담당 범위

## 1) 자연어 요청 분석

사용자가 입력한 문장을 분석해서 이동 선호 조건을 추출한다.

### 사용자 요청 예시

```text
조금 늦어도 덜 걷고 싶어
환승 적은 길로 가고 싶어
가장 빨리 도착하고 싶어
앉을 가능성이 높은 경로로 가고 싶어
비 와서 실외 이동이 적었으면 좋겠어
계단이나 오래 걷는 길은 피하고 싶어
```

### 분석 결과 예시

```json
{
  "timePriority": "medium",
  "walkPriority": "high",
  "transferPriority": "high",
  "comfortPriority": "medium",
  "allowDelay": true
}
```

---

## 2) 사용자 선호를 가중치로 변환

자연어 분석 결과를 실제 점수 계산에 사용할 수 있도록 숫자 가중치로 바꾼다.

### 예시

```json
{
  "timeWeight": 0.2,
  "walkWeight": 0.4,
  "transferWeight": 0.3,
  "comfortWeight": 0.1
}
```

이 가중치는 사용자의 요청에 따라 달라진다.

---

# 3. 요청 유형별 가중치 예시

| 사용자 요청 | 시간 | 도보 | 환승 | 쾌적도 |
|---|---:|---:|---:|---:|
| 가장 빨리 가고 싶어 | 0.6 | 0.2 | 0.1 | 0.1 |
| 조금 늦어도 덜 걷고 싶어 | 0.2 | 0.5 | 0.2 | 0.1 |
| 환승 적은 길로 가고 싶어 | 0.2 | 0.2 | 0.5 | 0.1 |
| 앉을 가능성이 높은 길로 가고 싶어 | 0.2 | 0.2 | 0.2 | 0.4 |
| 비 와서 실외 이동이 적었으면 좋겠어 | 0.2 | 0.45 | 0.2 | 0.15 |

---

# 4. 경로 후보 점수화

백엔드에서 받은 여러 경로 후보를 점수화한다.

## 경로 후보 예시

```json
[
  {
    "id": "A",
    "totalTime": 58,
    "walkDistance": 850,
    "transferCount": 2,
    "comfortScore": 60
  },
  {
    "id": "B",
    "totalTime": 64,
    "walkDistance": 420,
    "transferCount": 1,
    "comfortScore": 75
  },
  {
    "id": "C",
    "totalTime": 70,
    "walkDistance": 300,
    "transferCount": 2,
    "comfortScore": 70
  }
]
```

각 경로에 대해 시간, 도보 거리, 환승 횟수, 쾌적도 점수를 계산한다.

---

# 5. 기본 점수 계산 방식

각 항목을 0~100점으로 정규화한 뒤, 사용자 선호 가중치를 곱해서 최종 점수를 계산한다.

## 기본 공식

```text
최종 점수 =
시간 점수 × 시간 가중치
+ 도보 점수 × 도보 가중치
+ 환승 점수 × 환승 가중치
+ 쾌적도 점수 × 쾌적도 가중치
```

## 예시

사용자 요청:

```text
조금 늦어도 덜 걷고 싶고 환승은 적었으면 좋겠어.
```

가중치:

```json
{
  "timeWeight": 0.2,
  "walkWeight": 0.4,
  "transferWeight": 0.3,
  "comfortWeight": 0.1
}
```

이 경우 빠른 경로보다 도보 거리와 환승 횟수가 더 중요한 기준이 된다.

---

# 6. 정규화 방식

경로마다 단위가 다르기 때문에 단순 비교하면 안 된다.

예를 들어:

```text
시간: 분 단위
도보 거리: m 단위
환승 횟수: 회 단위
쾌적도: 0~100점
```

그래서 각 항목을 0~100점으로 변환한다.

## 시간 점수

가장 짧은 시간이 높은 점수를 받는다.

```text
timeScore = minTime / routeTime × 100
```

예시:

```text
최소 시간: 58분
A 경로: 58분 → 100점
B 경로: 64분 → 90.6점
C 경로: 70분 → 82.8점
```

---

## 도보 점수

도보 거리가 짧을수록 높은 점수를 받는다.

```text
walkScore = minWalkDistance / routeWalkDistance × 100
```

예시:

```text
최소 도보 거리: 300m
A 경로: 850m → 35.2점
B 경로: 420m → 71.4점
C 경로: 300m → 100점
```

---

## 환승 점수

환승 횟수가 적을수록 높은 점수를 받는다.

```text
transferScore = minTransferCount / routeTransferCount × 100
```

단, 환승 횟수가 0회일 수 있으므로 예외 처리가 필요하다.

현실적으로는 아래처럼 계산하는 것이 안전하다.

```text
transferScore = 100 - ((routeTransferCount - minTransferCount) × 25)
```

예시:

```text
최소 환승 횟수: 1회
A 경로: 2회 → 75점
B 경로: 1회 → 100점
C 경로: 2회 → 75점
```

---

## 쾌적도 점수

쾌적도는 별도 데이터가 없으면 간단한 규칙 기반으로 추정한다.

예시 기준:

```text
환승이 적을수록 쾌적도 증가
도보 거리가 짧을수록 쾌적도 증가
지하철 위주 경로는 버스 위주 경로보다 안정적이라고 가정
출퇴근 시간대 혼잡 구간은 쾌적도 감소
```

MVP에서는 정확한 실시간 혼잡도까지 구현하지 않아도 된다.  
대신 “추정 쾌적도”라는 이름으로 단순 규칙 기반 점수를 사용하면 된다.

---

# 7. 추천 경로 선택

각 경로의 최종 점수를 계산한 뒤, 가장 높은 점수를 받은 경로를 추천한다.

## 예시 결과

| 경로 | 시간 점수 | 도보 점수 | 환승 점수 | 쾌적도 | 최종 점수 |
|---|---:|---:|---:|---:|---:|
| A | 100 | 35.2 | 75 | 60 | 62.6 |
| B | 90.6 | 71.4 | 100 | 75 | 84.2 |
| C | 82.8 | 100 | 75 | 70 | 84.5 |

이 경우 C 경로가 가장 높은 점수를 받지만,  
B 경로와 점수 차이가 작고 B가 환승이 더 적다면 추천 이유에서 이를 설명할 수 있다.

예:

```text
C 경로는 도보 거리가 가장 짧지만, B 경로는 환승 횟수가 더 적고 전체 소요 시간도 짧아 종합적으로 더 균형 잡힌 경로입니다.
```

---

# 8. 추천 이유 생성

단순히 “B 경로 추천”이라고만 하면 서비스 가치가 약하다.  
왜 이 경로를 추천했는지 사용자가 이해할 수 있도록 문장으로 설명해야 한다.

## 추천 이유 예시

### 덜 걷기 요청

```text
이 경로는 최단 경로보다 6분 늦지만, 도보 거리가 430m 짧아 사용자의 ‘덜 걷고 싶다’는 요청에 더 적합합니다.
```

### 환승 적게 요청

```text
이 경로는 다른 경로보다 환승 횟수가 1회 적어 이동 중 갈아타는 부담이 적습니다.
```

### 빠른 도착 요청

```text
이 경로는 후보 경로 중 총 소요 시간이 가장 짧아 빠른 도착을 원하는 사용자에게 적합합니다.
```

### 쾌적도 요청

```text
이 경로는 도보 거리와 환승 횟수가 모두 적어 이동 피로도가 낮은 경로입니다.
```

---

# 9. AI 사용 여부

이 프로젝트에서 AI는 두 가지 방식으로 구현할 수 있다.

---

## 1안: 규칙 기반 자연어 분석

LLM 없이 키워드 기반으로 사용자의 요청을 분석한다.

### 예시

```text
"빨리", "최단", "급해" → 시간 가중치 증가
"덜 걷", "걷기 싫", "다리 아파" → 도보 가중치 증가
"환승 싫", "갈아타기 싫", "한 번에" → 환승 가중치 증가
"앉", "편하게", "쾌적" → 쾌적도 가중치 증가
"비", "눈", "추워", "더워" → 실외 도보 부담 증가
```

### 장점

- 구현이 빠르다.
- 비용이 없다.
- 결과가 예측 가능하다.
- 발표 때 알고리즘 설명이 쉽다.

### 단점

- 다양한 문장 표현에 약하다.
- 사용자가 복잡하게 말하면 분석이 부정확할 수 있다.

---

## 2안: LLM 기반 자연어 분석

OpenAI, Claude, Gemini, Bedrock 등을 사용해서 자연어 요청을 구조화한다.

### LLM 출력 예시

```json
{
  "intent": "comfort_route",
  "preferences": {
    "time": "medium",
    "walking": "high",
    "transfer": "high",
    "comfort": "medium"
  },
  "allowDelayMinutes": 10,
  "avoid": ["long_walk", "many_transfers"]
}
```

### 장점

- 자연어 이해력이 좋다.
- 다양한 표현을 처리할 수 있다.
- “AI 기반 서비스” 느낌이 강하다.

### 단점

- API 비용이 발생할 수 있다.
- 응답 지연이 생길 수 있다.
- JSON 형식 오류 처리가 필요하다.
- API 키 관리가 필요하다.
- 외부 API 장애에 영향을 받을 수 있다.

---

# 10. 추천 구현 방식

MVP에서는 아래 방식이 가장 안정적이다.

```text
1차: 규칙 기반 키워드 분석
2차: 여유가 있으면 LLM 기반 분석 추가
```

즉, 처음부터 LLM에 전부 의존하지 않는다.

## 추천 구조

```text
사용자 자연어 요청
 ↓
규칙 기반 분석
 ↓
선호 가중치 생성
 ↓
경로 점수화
 ↓
추천 결과 생성
```

시간이 남으면 아래처럼 확장한다.

```text
사용자 자연어 요청
 ↓
LLM으로 구조화 시도
 ↓
실패하면 규칙 기반 fallback
 ↓
선호 가중치 생성
 ↓
경로 점수화
 ↓
추천 결과 생성
```

이렇게 하면 안정성과 AI 활용성을 둘 다 챙길 수 있다.

---

# 11. AI & 알고리즘 최소 함수 목록

AI & 알고리즘 담당자는 아래 함수들을 구현하면 된다.

## 필수 함수

```text
analyzePreference(requestText)
```

사용자 자연어 요청을 분석해서 선호 조건을 추출한다.

---

```text
convertPreferenceToWeights(preference)
```

분석된 선호 조건을 점수 계산용 가중치로 변환한다.

---

```text
normalizeRoutes(routes)
```

경로 후보들의 시간, 도보 거리, 환승 횟수 등을 0~100점으로 정규화한다.

---

```text
calculateRouteScore(route, weights)
```

각 경로의 최종 점수를 계산한다.

---

```text
recommendRoute(routes, weights)
```

가장 적합한 경로를 선택한다.

---

```text
generateReason(recommendedRoute, routes, preference)
```

사용자에게 보여줄 추천 이유 문장을 생성한다.

---

# 12. 함수 흐름 예시

```text
requestText = "조금 늦어도 덜 걷고 싶어"

preference = analyzePreference(requestText)

weights = convertPreferenceToWeights(preference)

scoredRoutes = routes.map(route => calculateRouteScore(route, weights))

recommendedRoute = recommendRoute(scoredRoutes, weights)

reason = generateReason(recommendedRoute, scoredRoutes, preference)
```

---

# 13. 백엔드와 주고받을 데이터

AI & 알고리즘 모듈은 백엔드에서 아래 데이터를 받는다.

## 입력 데이터

```json
{
  "request": "조금 늦어도 덜 걷고 싶고 환승은 적었으면 좋겠어",
  "routes": [
    {
      "id": "A",
      "totalTime": 58,
      "walkDistance": 850,
      "transferCount": 2,
      "comfortScore": 60,
      "summary": "인하대 → 주안역 → 신도림역 → 강남역"
    },
    {
      "id": "B",
      "totalTime": 64,
      "walkDistance": 420,
      "transferCount": 1,
      "comfortScore": 75,
      "summary": "인하대 → 인천터미널 → 강남역"
    }
  ]
}
```

---

## 출력 데이터

```json
{
  "recommendedRouteId": "B",
  "reason": "최단 경로보다 6분 늦지만 도보 거리가 430m 짧고 환승이 1회 적어 사용자의 요청에 더 적합합니다.",
  "routes": [
    {
      "id": "A",
      "score": 62.6,
      "summary": "빠르지만 도보와 환승 부담이 큽니다."
    },
    {
      "id": "B",
      "score": 84.2,
      "summary": "시간은 조금 더 걸리지만 도보와 환승 부담이 적습니다."
    }
  ],
  "preference": {
    "timeWeight": 0.2,
    "walkWeight": 0.4,
    "transferWeight": 0.3,
    "comfortWeight": 0.1
  }
}
```

---

# 14. 백엔드와의 역할 경계

## 백엔드 담당

```text
외부 API를 호출해서 경로 후보를 가져온다.
경로 데이터를 정리해서 AI 모듈에 넘긴다.
AI 모듈 결과를 프론트엔드로 반환한다.
```

## AI & 알고리즘 담당

```text
사용자 요청을 분석한다.
경로 후보를 점수화한다.
추천 경로를 선택한다.
추천 이유를 생성한다.
```

즉, 백엔드는 데이터를 가져오고 연결하는 역할이고,  
AI & 알고리즘은 어떤 경로가 가장 적합한지 판단하는 역할이다.

---

# 15. 프론트엔드와의 연결

프론트엔드는 AI & 알고리즘 결과를 직접 호출하지 않는다.  
백엔드를 통해 받은 최종 결과를 화면에 표시한다.

프론트엔드가 화면에 표시할 주요 데이터는 다음과 같다.

```text
추천 경로 ID
추천 이유
경로별 점수
경로별 요약 설명
분석된 사용자 선호
```

프론트엔드는 이 데이터를 활용해 추천 경로 카드, 점수, 비교표, 추천 이유 박스를 만든다.

---

# 16. DB와의 연결

AI & 알고리즘 담당은 DB를 직접 다루지 않아도 된다.  
다만 어떤 데이터를 저장하면 좋을지 백엔드/AWS 담당에게 알려줘야 한다.

## 저장하면 좋은 데이터

```text
사용자 원문 요청
분석된 선호도
최종 가중치
경로 후보별 점수
추천된 경로 ID
추천 이유
```

이 데이터를 저장하면 발표 때 아래처럼 설명할 수 있다.

```text
사용자 요청과 추천 결과를 저장하여 이후 개인화 추천 성능 개선에 활용할 수 있는 구조로 설계했습니다.
```

---

# 17. AI & 알고리즘 최소 구현 범위

시간이 부족하면 아래 기능은 반드시 구현해야 한다.

## 필수 구현

```text
1. 자연어 요청 키워드 분석
2. 선호 조건 추출
3. 가중치 변환
4. 경로 후보 점수화
5. 최종 추천 경로 선택
6. 추천 이유 생성
```

이 정도만 구현해도 프로젝트의 차별점은 충분히 보여줄 수 있다.

---

# 18. AI & 알고리즘 추가 구현 범위

시간이 남으면 아래 기능을 추가한다.

## 선택 구현

```text
1. LLM 기반 자연어 분석
2. 규칙 기반 분석 실패 시 fallback 처리
3. 날씨 조건 반영
4. 사용자별 선호 저장 후 다음 추천에 반영
5. 시간대별 혼잡도 추정
6. 지하철/버스 선호 반영
7. 경로별 피로도 점수 계산
8. 추천 이유 문장 고도화
9. 테스트 케이스 자동화
```

---

# 19. 테스트 케이스

AI & 알고리즘 담당자는 추천 로직이 제대로 동작하는지 테스트해야 한다.

## 테스트 예시 1: 빠른 길 선호

### 입력

```text
가장 빨리 가고 싶어
```

### 기대 결과

```text
총 소요 시간이 가장 짧은 경로가 추천되어야 한다.
```

---

## 테스트 예시 2: 도보 최소 선호

### 입력

```text
조금 늦어도 덜 걷고 싶어
```

### 기대 결과

```text
도보 거리가 짧은 경로의 점수가 높아져야 한다.
```

---

## 테스트 예시 3: 환승 최소 선호

### 입력

```text
환승 적은 길로 가고 싶어
```

### 기대 결과

```text
환승 횟수가 적은 경로가 추천되어야 한다.
```

---

## 테스트 예시 4: 쾌적도 선호

### 입력

```text
편하게 가고 싶어
```

### 기대 결과

```text
도보 거리와 환승 횟수가 적은 경로의 점수가 높아져야 한다.
```

---

## 테스트 예시 5: 복합 조건

### 입력

```text
조금 늦어도 괜찮으니까 덜 걷고 환승도 적었으면 좋겠어
```

### 기대 결과

```text
시간보다 도보 거리와 환승 횟수가 더 높은 비중으로 반영되어야 한다.
```

---

# 20. 예외 처리

AI & 알고리즘은 애매한 요청도 처리해야 한다.

## 예외 상황

```text
사용자가 아무 요청도 입력하지 않은 경우
요청이 너무 짧은 경우
서로 충돌하는 조건을 입력한 경우
분석할 수 없는 문장을 입력한 경우
경로 후보가 1개뿐인 경우
경로 후보 데이터에 일부 값이 없는 경우
```

## 처리 방식

### 요청이 비어 있는 경우

```text
기본값: 시간 0.4 / 도보 0.25 / 환승 0.25 / 쾌적도 0.1
```

### 충돌 조건이 있는 경우

예:

```text
가장 빨리 가고 싶은데 절대 걷기는 싫어
```

처리:

```text
시간과 도보 가중치를 모두 높이고, 추천 이유에서 절충 경로라고 설명한다.
```

### 분석 불가능한 경우

```text
기본 추천 기준을 적용한다.
```

---

# 21. 발표에서 AI & 알고리즘이 설명할 내용

AI & 알고리즘 담당은 발표에서 아래 내용을 설명하면 좋다.

## 발표 포인트

```text
1. 기존 길찾기 서비스는 주로 최단시간 중심이다.
2. 하지만 실제 사용자는 덜 걷기, 환승 최소화, 쾌적함 등 다양한 조건을 고려한다.
3. 본 프로젝트는 사용자의 자연어 요청을 분석해 이동 선호도를 추출한다.
4. 추출한 선호도를 가중치로 변환한다.
5. 여러 경로 후보를 시간, 도보 거리, 환승 횟수, 쾌적도 기준으로 점수화한다.
6. 최종 점수가 가장 높은 경로를 추천한다.
7. 단순 추천이 아니라 왜 이 경로가 적합한지도 함께 설명한다.
```

---



# 23. 최종 산출물

AI & 알고리즘 담당이 최종적으로 제출하거나 시연할 수 있어야 하는 것은 다음과 같다.

```text
1. 자연어 요청 분석 로직
2. 선호 조건 추출 로직
3. 가중치 변환 로직
4. 경로 점수화 알고리즘
5. 최종 추천 경로 선택 로직
6. 추천 이유 생성 로직
7. 테스트 케이스
8. 백엔드와 연결 가능한 함수 또는 모듈
```

---

# 24. 정리

| 구분 | 내용 |
|---|---|
| 핵심 역할 | 자연어 요청 분석과 사용자 맞춤 경로 추천 |
| 입력 데이터 | 사용자 요청 문장, 경로 후보 목록 |
| 처리 과정 | 선호 분석 → 가중치 변환 → 점수화 → 추천 |
| 출력 데이터 | 추천 경로 ID, 경로별 점수, 추천 이유 |
| 필수 구현 | 키워드 기반 분석, 점수 계산, 추천 이유 생성 |
| 선택 구현 | LLM 분석, 날씨/혼잡도 반영, 개인화 고도화 |
| 최종 산출물 | 백엔드에서 호출 가능한 추천 알고리즘 모듈 |

AI & 알고리즘 파트는 이 프로젝트의 차별점을 만드는 핵심 역할이다.  
단순히 길찾기 API 결과를 보여주는 것이 아니라,  
**사용자가 말한 조건을 해석하고 그 조건에 맞게 경로를 다시 평가하는 부분**이 이 파트의 핵심이다.
