# 백엔드 담당 역할 정리

## 한 줄 정의

> 프론트엔드, 외부 대중교통 API, AI 추천 알고리즘, DB를 연결해 실제 서비스 요청이 동작하도록 만드는 역할

백엔드는 단순히 서버를 여는 역할이 아니라,  
사용자가 입력한 출발지·도착지·자연어 요청을 받아서  
**외부 경로 API 호출 → 데이터 정리 → AI 알고리즘 호출 → 최종 결과 반환**까지의 흐름을 담당한다.

---

# 1. 백엔드의 핵심 역할

백엔드 담당은 아래 흐름을 구현한다.

```text
프론트엔드 요청
 ↓
백엔드 API 서버
 ↓
외부 지도/대중교통 API 호출
 ↓
경로 후보 데이터 정리
 ↓
AI&알고리즘 모듈 호출
 ↓
추천 결과 생성
 ↓
프론트엔드로 JSON 응답 반환
```

즉, 백엔드는 프로젝트의 각 파트를 이어주는 **중간 연결 서버** 역할을 한다.

---

# 2. 담당 범위

## 1) API 서버 구현

프론트엔드가 호출할 수 있는 API를 만든다.

### 대표 API

```text
POST /api/recommend
```

프론트엔드에서 출발지, 도착지, 자연어 요청을 보내면  
백엔드는 외부 API와 AI 알고리즘을 거쳐 추천 결과를 반환한다.

### 요청 예시

```json
{
  "start": "인하대학교",
  "end": "강남역",
  "request": "조금 늦어도 덜 걷고 싶고 환승은 적었으면 좋겠어"
}
```

### 응답 예시

```json
{
  "recommendedRouteId": "B",
  "reason": "최단 경로보다 6분 늦지만 도보 거리가 430m 짧고 환승이 1회 적어 사용자의 요청에 더 적합합니다.",
  "routes": [
    {
      "id": "A",
      "title": "가장 빠른 경로",
      "totalTime": 58,
      "walkDistance": 850,
      "transferCount": 2,
      "score": 78,
      "summary": "빠르지만 도보와 환승 부담이 큽니다."
    },
    {
      "id": "B",
      "title": "AI 추천 경로",
      "totalTime": 64,
      "walkDistance": 420,
      "transferCount": 1,
      "score": 91,
      "summary": "시간은 조금 더 걸리지만 도보와 환승 부담이 적습니다."
    }
  ]
}
```

---

## 2) 외부 API 연동

백엔드는 지도/대중교통 관련 외부 API를 호출한다.

### 후보 API

- ODsay API
- TMAP 대중교통 API
- 네이버지도 API
- 기타 좌표 변환 / 장소 검색 API

### 백엔드가 처리해야 하는 것

- 출발지/도착지를 좌표로 변환
- 좌표 기반 대중교통 경로 요청
- 외부 API 응답 데이터 수신
- 필요한 데이터만 추출
- 프론트와 AI 알고리즘이 쓰기 좋은 형태로 정리

### 외부 API 응답에서 추출할 데이터

- 총 소요 시간
- 도보 거리
- 환승 횟수
- 이동 수단
- 지하철/버스 노선명
- 출발역/도착역
- 경로 요약
- 예상 도착 시간

---

## 3) 경로 데이터 정규화

외부 API 응답은 복잡하므로, 백엔드가 내부에서 쓰기 쉬운 형태로 바꿔야 한다.

### 정규화 전

외부 API마다 응답 구조가 다르고 복잡하다.

```text
ODsay 응답 구조
TMAP 응답 구조
네이버 API 응답 구조
```

각 API의 응답 형식이 다르기 때문에 프론트엔드와 AI 알고리즘이 직접 처리하기 어렵다.

### 정규화 후

백엔드는 모든 경로 데이터를 아래와 같은 공통 형식으로 정리한다.

```json
{
  "id": "A",
  "totalTime": 58,
  "walkDistance": 850,
  "transferCount": 2,
  "transportTypes": ["subway", "bus"],
  "pathSummary": "인하대 → 주안역 → 신도림역 → 강남역"
}
```

이렇게 하면 AI 알고리즘 담당은 API 종류와 상관없이 같은 구조의 데이터를 받아 점수화할 수 있다.

---

## 4) AI&알고리즘 모듈 연결

백엔드는 AI&알고리즘 담당자가 만든 추천 로직을 호출한다.

### 백엔드가 AI 모듈에 넘기는 데이터

```json
{
  "request": "조금 늦어도 덜 걷고 싶고 환승은 적었으면 좋겠어",
  "routes": [
    {
      "id": "A",
      "totalTime": 58,
      "walkDistance": 850,
      "transferCount": 2
    },
    {
      "id": "B",
      "totalTime": 64,
      "walkDistance": 420,
      "transferCount": 1
    }
  ]
}
```

### AI 모듈이 백엔드에 반환하는 데이터

```json
{
  "recommendedRouteId": "B",
  "reason": "최단 경로보다 6분 늦지만 도보 거리가 430m 짧고 환승이 1회 적어 사용자의 요청에 더 적합합니다.",
  "routes": [
    {
      "id": "A",
      "score": 78,
      "summary": "빠르지만 도보와 환승 부담이 큽니다."
    },
    {
      "id": "B",
      "score": 91,
      "summary": "시간은 조금 더 걸리지만 도보와 환승 부담이 적습니다."
    }
  ]
}
```

백엔드는 이 결과를 프론트엔드가 바로 표시할 수 있는 최종 JSON으로 정리해서 반환한다.

---

# 3. DB 관련 역할

DB는 AWS 담당과 백엔드 담당이 나눠서 처리한다.

## 백엔드 담당이 하는 일

- DB 연결 코드 작성
- 검색 기록 저장
- 사용자 요청 저장
- 추천 결과 저장
- 저장된 기록 조회 API 구현

## AWS 담당이 하는 일

- RDS 또는 DynamoDB 생성
- 보안그룹 설정
- DB 접속 정보 관리
- 환경변수 설정
- AWS 인프라 연결

즉, 역할은 이렇게 나뉜다.

```text
AWS 담당: DB를 만든다
백엔드 담당: DB를 코드에서 사용한다
```

---

# 4. 저장하면 좋은 데이터

프로젝트에서 DB를 사용한다면 최소한 아래 데이터를 저장하면 좋다.

## 검색 기록

- 출발지
- 도착지
- 자연어 요청
- 추천된 경로
- 검색 시간

## 사용자 선호 정보

- 빠른 길 선호
- 도보 적은 길 선호
- 환승 적은 길 선호
- 앉을 가능성 높은 길 선호

## 추천 결과 로그

- 경로 후보 개수
- 추천된 경로 ID
- 추천 점수
- 추천 이유
- API 응답 성공/실패 여부

---

# 5. 백엔드 최소 API 목록

시간이 부족하면 최소한 아래 API만 구현해도 된다.

## 필수 API

```text
POST /api/recommend
```

사용자가 입력한 출발지, 도착지, 자연어 요청을 받아 추천 경로를 반환한다.

---

## 선택 API

```text
GET /api/health
```

서버가 정상 동작 중인지 확인하는 API.

```text
GET /api/history
```

사용자 검색 기록 조회 API.

```text
POST /api/history
```

검색 기록 저장 API.

```text
GET /api/preferences
```

사용자 선호 설정 조회 API.

```text
POST /api/preferences
```

사용자 선호 설정 저장 API.

---

# 6. 백엔드 최소 구현 범위

시간이 부족하면 아래 기능은 반드시 구현해야 한다.

## 필수 구현

- API 서버 실행
- `/api/recommend` 구현
- 프론트엔드 요청 수신
- 외부 대중교통 API 호출
- 경로 후보 데이터 정리
- AI 알고리즘 모듈 호출
- 최종 추천 결과 JSON 반환
- 에러 처리

이 정도가 구현되면 서비스의 핵심 흐름은 동작한다.

---

# 7. 백엔드 추가 구현 범위

시간이 남으면 아래 기능을 추가한다.

## 선택 구현

- 검색 기록 DB 저장
- 사용자 선호 설정 저장
- API 호출 실패 시 fallback 데이터 반환
- 외부 API 응답 캐싱
- 요청/응답 로그 저장
- API 응답 시간 측정
- Swagger 또는 API 명세 문서 작성
- CloudWatch 로그 연동 지원

---

# 8. 백엔드와 프론트엔드의 연결

프론트엔드는 백엔드에 아래 형태로 요청한다.

```json
{
  "start": "인하대학교",
  "end": "강남역",
  "request": "조금 늦어도 덜 걷고 싶어"
}
```

백엔드는 아래 형태로 응답한다.

```json
{
  "recommendedRouteId": "B",
  "reason": "최단 경로보다 6분 늦지만 도보 거리가 짧아 추천합니다.",
  "routes": [
    {
      "id": "A",
      "title": "가장 빠른 경로",
      "totalTime": 58,
      "walkDistance": 850,
      "transferCount": 2,
      "score": 78
    },
    {
      "id": "B",
      "title": "AI 추천 경로",
      "totalTime": 64,
      "walkDistance": 420,
      "transferCount": 1,
      "score": 91
    }
  ]
}
```

프론트엔드는 이 응답을 받아 결과 카드와 추천 이유를 화면에 표시한다.

---

# 9. 백엔드와 AI&알고리즘의 연결

AI&알고리즘 담당자는 추천 로직을 함수 형태로 제공한다.

예시 함수:

```text
analyzePreference(request)
calculateRouteScore(routes, preference)
recommendRoute(routes, preference)
generateReason(recommendedRoute, routes, preference)
```

백엔드는 이 함수를 호출해서 최종 추천 결과를 만든다.

```text
백엔드 역할:
- 외부 API에서 경로 후보를 가져온다
- AI 알고리즘에 경로 후보를 넘긴다
- AI 알고리즘 결과를 받아 프론트에 반환한다
```

---

# 10. 백엔드와 AWS의 연결

AWS 담당자는 서버가 배포될 환경을 만들고,  
백엔드 담당자는 그 환경에서 서버가 정상적으로 실행되도록 맞춘다.

## 백엔드 담당

- 서버 코드 작성
- 환경변수 사용
- API Key 코드에 직접 노출하지 않기
- DB 연결 코드 작성
- 배포 후 오류 수정

## AWS 담당

- EC2 생성
- 보안그룹 설정
- RDS/DynamoDB 생성
- 환경변수 설정
- 서버 실행 환경 구성
- CloudWatch 로그 확인

---

# 11. 에러 처리

백엔드는 외부 API나 AI 알고리즘이 실패했을 때도 프론트엔드가 깨지지 않도록 처리해야 한다.

## 처리해야 할 에러

- 출발지/도착지 입력 누락
- 외부 API 응답 실패
- 경로 후보 없음
- AI 알고리즘 오류
- DB 저장 실패
- 서버 내부 오류

## 에러 응답 예시

```json
{
  "error": true,
  "message": "경로를 찾을 수 없습니다. 출발지와 도착지를 다시 확인해주세요."
}
```

---

# 12. 발표에서 백엔드가 설명할 내용

백엔드 담당은 발표에서 아래 내용을 설명하면 좋다.

## 발표 포인트

- 프론트엔드 요청을 받아 추천 결과를 반환하는 API 서버를 구현했다.
- 외부 대중교통 API를 연동해 실제 경로 후보를 가져왔다.
- 외부 API 응답을 내부 공통 데이터 구조로 변환했다.
- AI 추천 알고리즘 모듈과 연결해 사용자 맞춤형 경로를 반환했다.
- 검색 기록과 추천 결과를 DB에 저장할 수 있는 구조로 설계했다.
- AWS 배포 환경에서도 동작할 수 있도록 환경변수와 API 구조를 분리했다.

---

# 13. 백엔드 담당의 최종 산출물

백엔드 담당이 최종적으로 제출하거나 시연할 수 있어야 하는 것은 다음과 같다.

```text
1. API 서버
2. /api/recommend 엔드포인트
3. 외부 대중교통 API 연동 코드
4. 경로 데이터 정규화 로직
5. AI 알고리즘 모듈 연결
6. 최종 추천 결과 JSON 응답
7. 에러 처리
8. DB 저장 로직
```

---

# 14. 팀원에게 설명할 문장

회의 때는 아래처럼 설명하면 된다.

> 백엔드는 프론트엔드, 외부 대중교통 API, AI 추천 알고리즘, DB를 연결하는 서버 역할을 담당한다. 사용자가 입력한 출발지·도착지·자연어 요청을 받아 경로 후보를 조회하고, AI 알고리즘을 통해 추천 경로를 선택한 뒤, 프론트엔드가 바로 표시할 수 있는 JSON 형태로 반환한다.

---

# 15. 정리

| 구분 | 내용 |
|---|---|
| 핵심 역할 | 프론트, 외부 API, AI 알고리즘, DB 연결 |
| 필수 API | POST /api/recommend |
| 필수 기능 | 요청 수신, 외부 API 호출, 데이터 정리, AI 모듈 호출, 결과 반환 |
| DB 역할 | DB를 코드에서 사용하고 검색 기록/추천 결과 저장 |
| AWS와의 경계 | AWS 담당은 인프라 생성, 백엔드는 서버 코드와 DB 사용 |
| 최종 산출물 | 실제 추천 결과를 반환하는 API 서버 |

백엔드는 프로젝트의 각 기능을 실제로 이어주는 중심 역할이다.  
프론트엔드가 화면을 만들고, AI 알고리즘이 추천 로직을 만들더라도,  
백엔드가 제대로 연결하지 않으면 서비스는 동작하지 않는다.
